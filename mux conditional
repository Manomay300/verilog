module mux4_1 (
    input  [3:0] I,   // Inputs I0=I[0], I1=I[1], I2=I[2], I3=I[3]
    input  [1:0] S,   // Select lines
    output       Y
);

assign Y = (S == 2'b00) ? I[0] :
           (S == 2'b01) ? I[1] :
           (S == 2'b10) ? I[2] :
                          I[3];

endmodule
